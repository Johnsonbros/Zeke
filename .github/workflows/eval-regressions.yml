name: Eval regressions

on:
  push:
    branches: ["**"]
  pull_request:

jobs:
  run-evals:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -e .

      - name: Run eval suite
        run: python eval/runner.py --ci

      - name: Upload eval results to OpenAI Evals
        env:
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
          OPENAI_EVAL_GUID: ${{ secrets.OPENAI_EVAL_GUID }}
        run: |
          if [ -n "$OPENAI_API_KEY" ]; then
            python eval/runner.py --openai-evals
          else
            echo "OPENAI_API_KEY is not configured; skipping optional OpenAI Evals upload."
          fi

      - name: Upload eval run metadata
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: eval-runs
          path: eval/runs/*.json
          if-no-files-found: warn
